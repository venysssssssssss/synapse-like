[package]
name = "synapse-native"
version = "0.1.0"
edition = "2021"

[lib]
name = "synapse_native"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
libc = "0.2"

[profile.release]
lto = true
codegen-units = 1
strip = true
